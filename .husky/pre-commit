#!/usr/bin/env sh
set -e

echo "husky: pre-commit - lint / type-check / test"

# Bun が必須。見つからない場合はエラーにする。
if ! command -v bun >/dev/null 2>&1; then
  echo "Error: bun not found. Please install Bun (https://bun.sh)" >&2
  exit 1
fi

bun run precommit:check

